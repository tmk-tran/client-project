import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    body1: {
      fontSize: 16,
      fontWeight: "bold",
      textAlign: "center",
    },
  },
});

export default theme;